/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export',
    reactStrictMode: false,
    // distDir: 'build'
    // outputFileTracing: false
}

module.exports = nextConfig
